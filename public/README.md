# Static assets

Files here are served by the bot's HTTP server under `/assets/…` (e.g. `public/assets/logo.png`
→ `https://bot.edmorex.com/assets/logo.png`). The `public/` directory is copied into the Docker
image at build time.